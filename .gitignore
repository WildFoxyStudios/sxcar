# Rust
/backend/target
**/target
# Node
node_modules
.pnpm-store
.turbo
dist
.expo
# Env / secretos
.env
.env.*
!.env.example
# Terraform / IaC (estado y secretos NUNCA a git)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# (.terraform.lock.hcl SÍ se commitea: pinea hashes de providers = builds reproducibles/seguros)
# Vercel
.vercel
# OS / editores
.DS_Store
Thumbs.db
# Config local de herramientas (no es parte de la app)
.mcp.json
