# F0.1 — Scaffolding del monorepo + backend Rust + CI — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Crear el esqueleto del monorepo y un backend Rust (`axum`) con un endpoint `/health` que verifica la conexión a Postgres, todo cubierto por tests y un pipeline de CI verde.

**Architecture:** Cargo workspace con una lib `db` (pool sqlx + ping) y un binario `api` (axum) que expone `/health`. Infra local vía Docker Compose (Postgres+PostGIS y Redis). CI en GitHub Actions levanta Postgres como service y corre fmt/clippy/test/audit.

**Tech Stack:** Rust stable (axum 0.7, tokio 1, sqlx 0.8, serde 1, tracing 0.1), Docker Compose, GitHub Actions, pnpm + Turborepo (estructura; el cableado JS completo llega en F0.4).

## Global Constraints

Copiado del spec maestro (`docs/superpowers/specs/2026-06-25-grindr-clone-design.md`). Aplica a todas las tareas:

- **Backend:** Rust (`axum` + `tokio`), binarios dockerizados, target de despliegue VPS AWS EC2 (Graviton/ARM).
- **DB:** Neon (PostgreSQL + PostGIS) como fuente de verdad. Acceso vía **sqlx**; **sin SQL crudo** en lógica de negocio (el ping de salud es la única sentencia literal permitida). Las queries verificadas en compilación (`sqlx::query!` + `cargo sqlx prepare` para CI offline) entran en F0.2 junto al esquema.
- **Caché/realtime/geo:** Redis autoalojado.
- **Media:** Cloudflare R2.
- **Monorepo:** pnpm workspaces + Turborepo (JS); Cargo workspace (Rust).
- **Seguridad (0 vuln conocidas):** `cargo audit` en CI (obligatorio, falla el build ante vulnerabilidades); `clippy` con `-D warnings`; secretos solo por entorno, nunca en el repo.
- **Tarpit (honeypot, decisión del equipo):** incluido en F0.1 pero **acotado por diseño** — solo se activa en rutas-cebo (nunca rutas legítimas), con tope global de conexiones simultáneas (semáforo), límites de bytes/tiempo por conexión y kill-switch por entorno (`TARPIT_ENABLED`). Invariante: el tarpit **nunca** debe poder agotar recursos del origen.
- **Metodología:** TDD por defecto; commits frecuentes (uno por tarea como mínimo).
- **Versiones piso:** Rust stable, Node ≥ 20, pnpm ≥ 9.

## Prerequisites (entorno del ejecutor)

- Rust stable (`rustup`), Docker + Docker Compose, Git, Node ≥ 20 con `corepack enable` (pnpm).
- Las pruebas de integración usan `TEST_DATABASE_URL` (por defecto `postgres://dev:dev@localhost:5432/appdb`). Levanta la DB con Docker Compose (Task 2) antes de correr tests que tocan Postgres.

---

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `.gitignore` | Ignorar `target/`, `node_modules/`, `.env`, etc. |
| `README.md` | Punto de entrada del repo |
| `package.json` (raíz) | Workspace JS (privado, packageManager pnpm) |
| `pnpm-workspace.yaml` | Globs de paquetes JS (`apps/*`, `packages/*`) |
| `infra/docker-compose.yml` | Postgres+PostGIS y Redis locales |
| `backend/Cargo.toml` | Definición del Cargo workspace |
| `backend/rust-toolchain.toml` | Pin de toolchain stable + componentes |
| `backend/crates/db/Cargo.toml` | Deps de la lib `db` |
| `backend/crates/db/src/lib.rs` | `connect()` (pool) + `ping()` |
| `backend/crates/db/tests/connection.rs` | Test de integración de conexión |
| `backend/crates/api/Cargo.toml` | Deps del binario `api` |
| `backend/crates/api/src/config.rs` | Carga de configuración por entorno |
| `backend/crates/api/src/health.rs` | Handler `/health` (consulta DB) |
| `backend/crates/api/src/tarpit.rs` | HTTP tarpit acotado (honeypot) + tests del gating |
| `backend/crates/api/src/lib.rs` | `app(pool)` → `Router` + `AppState` |
| `backend/crates/api/src/main.rs` | Bootstrap del binario |
| `backend/crates/api/tests/health.rs` | Test de integración del endpoint |
| `backend/Dockerfile` | Build multi-stage del binario `api` |
| `.github/workflows/ci.yml` | Pipeline CI (fmt/clippy/test/audit) |

---

### Task 1: Esqueleto del repositorio

**Files:**
- Create: `.gitignore`, `README.md`, `package.json`, `pnpm-workspace.yaml`

**Interfaces:**
- Consumes: nada.
- Produces: estructura de carpetas raíz y repo git inicializado.

- [ ] **Step 1: Inicializar git y carpetas base**

```bash
cd "C:/Users/echev/Desktop/proyecto-X"
git init
mkdir -p apps packages backend/crates infra .github/workflows
```

- [ ] **Step 2: Crear `.gitignore`**

```gitignore
# Rust
/backend/target
**/target
# Node
node_modules
.pnpm-store
.turbo
dist
.expo
# Env / secretos
.env
.env.*
!.env.example
# OS / editores
.DS_Store
Thumbs.db
```

- [ ] **Step 3: Crear `README.md`**

```markdown
# proyecto-X

App social geolocalizada (paridad Grindr). Monorepo: `apps/` (RN + Next.js), `packages/` (TS compartido), `backend/` (Rust workspace), `infra/` (Docker/Terraform).

Ver `docs/superpowers/specs/` para el spec maestro y `docs/superpowers/plans/` para los planes por fase.

## Desarrollo local
1. `docker compose -f infra/docker-compose.yml up -d` (Postgres + Redis)
2. `cd backend && cargo run --bin api`
3. `curl http://localhost:8080/health`
```

- [ ] **Step 4: Crear `package.json` raíz**

```json
{
  "name": "proyecto-x",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
```

- [ ] **Step 5: Crear `pnpm-workspace.yaml`**

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

- [ ] **Step 6: Verificar y commitear**

Run: `git add -A && git status --short`
Expected: lista los archivos nuevos en staging (`.gitignore`, `README.md`, `package.json`, `pnpm-workspace.yaml`).

```bash
git commit -m "chore: inicializar esqueleto del monorepo"
```

---

### Task 2: Infra local (Docker Compose)

**Files:**
- Create: `infra/docker-compose.yml`

**Interfaces:**
- Consumes: nada.
- Produces: servicios locales `postgres` (con PostGIS) en `localhost:5432` y `redis` en `localhost:6379`. Credenciales dev: `dev:dev`, DB `appdb`.

- [ ] **Step 1: Crear `infra/docker-compose.yml`**

```yaml
services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: appdb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev -d appdb"]
      interval: 5s
      timeout: 5s
      retries: 10
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  pgdata:
```

- [ ] **Step 2: Levantar servicios y verificar Postgres**

Run: `docker compose -f infra/docker-compose.yml up -d`
Then: `docker compose -f infra/docker-compose.yml ps`
Expected: `postgres` y `redis` en estado `running`; `postgres` reporta `healthy` tras unos segundos.

- [ ] **Step 3: Comprobar PostGIS disponible**

Run: `docker compose -f infra/docker-compose.yml exec postgres psql -U dev -d appdb -c "SELECT PostGIS_Version();"`
Expected: imprime una versión de PostGIS (p. ej. `3.4 ...`).

- [ ] **Step 4: Commit**

```bash
git add infra/docker-compose.yml
git commit -m "chore: docker-compose con Postgres+PostGIS y Redis para desarrollo local"
```

---

### Task 3: Cargo workspace + lib `db` (connect + ping)

**Files:**
- Create: `backend/Cargo.toml`, `backend/rust-toolchain.toml`, `backend/crates/db/Cargo.toml`, `backend/crates/db/src/lib.rs`, `backend/crates/db/tests/connection.rs`

**Interfaces:**
- Consumes: servicio Postgres de Task 2 (`TEST_DATABASE_URL`).
- Produces:
  - `db::Pool` = alias de `sqlx::PgPool`.
  - `async fn db::connect(database_url: &str) -> anyhow::Result<db::Pool>`.
  - `async fn db::ping(pool: &db::Pool) -> anyhow::Result<()>`.

- [ ] **Step 1: Crear el manifiesto del workspace `backend/Cargo.toml`**

```toml
[workspace]
resolver = "2"
members = ["crates/db", "crates/api"]

[workspace.package]
edition = "2021"
license = "UNLICENSED"

[workspace.dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "tls-rustls", "postgres", "macros", "migrate"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1"
thiserror = "1"
tower = { version = "0.5", features = ["util"] }
tower-http = { version = "0.6", features = ["trace", "cors"] }
http-body-util = "0.1"
```

- [ ] **Step 2: Pin de toolchain `backend/rust-toolchain.toml`**

```toml
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
```

- [ ] **Step 3: Manifiesto de la lib `backend/crates/db/Cargo.toml`**

```toml
[package]
name = "db"
version = "0.1.0"
edition.workspace = true

[dependencies]
sqlx.workspace = true
anyhow.workspace = true
tracing.workspace = true

[dev-dependencies]
tokio.workspace = true
```

- [ ] **Step 4: Escribir el test de integración que falla `backend/crates/db/tests/connection.rs`**

```rust
use db::{connect, ping};

fn test_db_url() -> String {
    std::env::var("TEST_DATABASE_URL")
        .unwrap_or_else(|_| "postgres://dev:dev@localhost:5432/appdb".to_string())
}

#[tokio::test]
async fn connect_and_ping_succeeds() {
    let pool = connect(&test_db_url()).await.expect("should connect to postgres");
    ping(&pool).await.expect("ping should succeed");
}
```

- [ ] **Step 5: Correr el test para confirmar que falla**

Run: `cd backend && cargo test -p db`
Expected: FALLA en compilación — `error[E0432]: unresolved import` / `cannot find function connect in crate db` (aún no existe `src/lib.rs`).

- [ ] **Step 6: Implementar `backend/crates/db/src/lib.rs`**

```rust
use sqlx::postgres::PgPoolOptions;

pub type Pool = sqlx::PgPool;

/// Crea un pool de conexiones a Postgres.
pub async fn connect(database_url: &str) -> anyhow::Result<Pool> {
    let pool = PgPoolOptions::new()
        .max_connections(10)
        .connect(database_url)
        .await?;
    Ok(pool)
}

/// Comprobación de salud de la base de datos.
pub async fn ping(pool: &Pool) -> anyhow::Result<()> {
    sqlx::query("SELECT 1").execute(pool).await?;
    Ok(())
}
```

- [ ] **Step 7: Correr el test para confirmar que pasa**

Run: `cd backend && cargo test -p db`
Expected: PASA — `test connect_and_ping_succeeds ... ok` (requiere Postgres de Task 2 levantado).

- [ ] **Step 8: Commit**

```bash
git add backend/Cargo.toml backend/rust-toolchain.toml backend/crates/db
git commit -m "feat(db): lib con pool sqlx y health ping a Postgres"
```

---

### Task 4: Configuración por entorno en `api`

**Files:**
- Create: `backend/crates/api/Cargo.toml`, `backend/crates/api/src/config.rs`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `struct api::config::Config { pub database_url: String, pub bind_addr: String }`.
  - `Config::from_env() -> anyhow::Result<Config>`.
  - `Config::from_getter(get: impl Fn(&str) -> Option<String>) -> anyhow::Result<Config>` (inyección para tests, sin tocar el entorno del proceso).

- [ ] **Step 1: Manifiesto del binario `backend/crates/api/Cargo.toml`**

```toml
[package]
name = "api"
version = "0.1.0"
edition.workspace = true

[[bin]]
name = "api"
path = "src/main.rs"

[lib]
name = "api"
path = "src/lib.rs"

[dependencies]
axum.workspace = true
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
anyhow.workspace = true
tower-http.workspace = true
db = { path = "../db" }

[dev-dependencies]
tower.workspace = true
http-body-util.workspace = true
```

- [ ] **Step 2: Escribir el módulo y su test que falla `backend/crates/api/src/config.rs`**

```rust
#[derive(Clone, Debug)]
pub struct Config {
    pub database_url: String,
    pub bind_addr: String,
}

impl Config {
    pub fn from_env() -> anyhow::Result<Self> {
        Self::from_getter(|k| std::env::var(k).ok())
    }

    pub fn from_getter(get: impl Fn(&str) -> Option<String>) -> anyhow::Result<Self> {
        let database_url = get("DATABASE_URL")
            .ok_or_else(|| anyhow::anyhow!("DATABASE_URL must be set"))?;
        let bind_addr = get("BIND_ADDR").unwrap_or_else(|| "0.0.0.0:8080".to_string());
        Ok(Self { database_url, bind_addr })
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn defaults_bind_addr_when_unset() {
        let c = Config::from_getter(|k| match k {
            "DATABASE_URL" => Some("postgres://x".into()),
            _ => None,
        })
        .unwrap();
        assert_eq!(c.database_url, "postgres://x");
        assert_eq!(c.bind_addr, "0.0.0.0:8080");
    }

    #[test]
    fn errors_without_database_url() {
        assert!(Config::from_getter(|_| None).is_err());
    }
}
```

- [ ] **Step 3: Crear un `lib.rs` mínimo para que el módulo compile**

Crear `backend/crates/api/src/lib.rs` con solo el módulo de config por ahora (Task 5 lo amplía):

```rust
pub mod config;
```

- [ ] **Step 4: Correr los tests para confirmar que pasan**

Run: `cd backend && cargo test -p api config`
Expected: PASA — `defaults_bind_addr_when_unset ... ok` y `errors_without_database_url ... ok`.

- [ ] **Step 5: Commit**

```bash
git add backend/crates/api/Cargo.toml backend/crates/api/src/config.rs backend/crates/api/src/lib.rs
git commit -m "feat(api): carga de configuración por entorno con tests"
```

---

### Task 5: Endpoint `/health` con verificación de DB + binario `api`

**Files:**
- Modify: `backend/crates/api/src/lib.rs`
- Create: `backend/crates/api/src/health.rs`, `backend/crates/api/src/main.rs`, `backend/crates/api/tests/health.rs`

**Interfaces:**
- Consumes: `db::{connect, ping, Pool}`; `api::config::Config`.
- Produces:
  - `#[derive(Clone)] struct api::AppState { pub pool: db::Pool }`.
  - `fn api::app(pool: db::Pool) -> axum::Router`.
  - `GET /health` → `200 {"status":"ok","db":"up"}` si la DB responde; `503 {"status":"degraded","db":"down"}` si no.

- [ ] **Step 1: Escribir el test de integración que falla `backend/crates/api/tests/health.rs`**

```rust
use axum::body::Body;
use axum::http::{Request, StatusCode};
use http_body_util::BodyExt;
use tower::ServiceExt; // aporta `oneshot`

fn test_db_url() -> String {
    std::env::var("TEST_DATABASE_URL")
        .unwrap_or_else(|_| "postgres://dev:dev@localhost:5432/appdb".to_string())
}

#[tokio::test]
async fn health_reports_ok_and_db_up() {
    let pool = db::connect(&test_db_url()).await.expect("connect");
    let app = api::app(pool);

    let response = app
        .oneshot(
            Request::builder()
                .uri("/health")
                .body(Body::empty())
                .unwrap(),
        )
        .await
        .unwrap();

    assert_eq!(response.status(), StatusCode::OK);

    let bytes = response.into_body().collect().await.unwrap().to_bytes();
    let json: serde_json::Value = serde_json::from_slice(&bytes).unwrap();
    assert_eq!(json["status"], "ok");
    assert_eq!(json["db"], "up");
}
```

- [ ] **Step 2: Correr el test para confirmar que falla**

Run: `cd backend && cargo test -p api --test health`
Expected: FALLA en compilación — `cannot find function app in crate api` (aún no existe).

- [ ] **Step 3: Implementar el handler `backend/crates/api/src/health.rs`**

```rust
use axum::{extract::State, http::StatusCode, Json};
use serde::Serialize;

use crate::AppState;

#[derive(Serialize)]
pub struct Health {
    pub status: &'static str,
    pub db: &'static str,
}

pub async fn health(State(state): State<AppState>) -> (StatusCode, Json<Health>) {
    match db::ping(&state.pool).await {
        Ok(()) => (StatusCode::OK, Json(Health { status: "ok", db: "up" })),
        Err(_) => (
            StatusCode::SERVICE_UNAVAILABLE,
            Json(Health { status: "degraded", db: "down" }),
        ),
    }
}
```

- [ ] **Step 4: Ampliar `backend/crates/api/src/lib.rs`**

Reemplazar el contenido completo del archivo por:

```rust
pub mod config;
pub mod health;

use axum::{routing::get, Router};
use db::Pool;

#[derive(Clone)]
pub struct AppState {
    pub pool: Pool,
}

pub fn app(pool: Pool) -> Router {
    let state = AppState { pool };
    Router::new()
        .route("/health", get(health::health))
        .with_state(state)
}
```

- [ ] **Step 5: Implementar el binario `backend/crates/api/src/main.rs`**

```rust
use api::config::Config;

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    tracing_subscriber::fmt()
        .with_env_filter(
            tracing_subscriber::EnvFilter::try_from_default_env()
                .unwrap_or_else(|_| "info".into()),
        )
        .init();

    let config = Config::from_env()?;
    let pool = db::connect(&config.database_url).await?;
    let app = api::app(pool);

    let listener = tokio::net::TcpListener::bind(&config.bind_addr).await?;
    tracing::info!("api listening on {}", config.bind_addr);
    axum::serve(listener, app).await?;
    Ok(())
}
```

- [ ] **Step 6: Correr el test para confirmar que pasa**

Run: `cd backend && cargo test -p api --test health`
Expected: PASA — `health_reports_ok_and_db_up ... ok`.

- [ ] **Step 7: Smoke test manual del binario**

Run (con Docker Compose arriba):
```bash
cd backend && DATABASE_URL=postgres://dev:dev@localhost:5432/appdb cargo run --bin api
```
En otra terminal: `curl -s http://localhost:8080/health`
Expected: `{"status":"ok","db":"up"}`. Detener con Ctrl+C.

- [ ] **Step 8: Verificar formato y lint antes de commitear**

Run: `cd backend && cargo fmt --all && cargo clippy --all-targets -- -D warnings`
Expected: sin cambios de formato pendientes y clippy sin warnings.

- [ ] **Step 9: Commit**

```bash
git add backend/crates/api
git commit -m "feat(api): endpoint /health con verificación de DB y binario axum"
```

---

### Task 6: HTTP Tarpit acotado (honeypot anti-escáneres)

> Incluido por decisión del equipo. Implementado con **límites duros** para no violar los objetivos de coste y 0-vuln: solo rutas-cebo, tope global por semáforo (declina si está lleno), límites de bytes/tiempo y kill-switch.

**Files:**
- Create: `backend/crates/api/src/tarpit.rs`
- Modify: `backend/crates/api/src/lib.rs` (registrar rutas-cebo + `Tarpit` en `AppState`)
- Modify: `backend/crates/api/Cargo.toml` (añadir `async-stream`)

**Interfaces:**
- Consumes: `crate::AppState`.
- Produces:
  - `struct api::tarpit::TarpitConfig { enabled: bool, max_concurrent: usize, drip_interval: Duration, max_bytes: usize }` con `Default` y `from_env()`.
  - `enum api::tarpit::TarpitDecision { Trap(tokio::sync::OwnedSemaphorePermit), Decline }`.
  - `struct api::tarpit::Tarpit` con `new(TarpitConfig)`, `try_trap() -> TarpitDecision`, `available() -> usize` y campo público `config`.
  - `async fn api::tarpit::handler(State<AppState>) -> axum::response::Response`.
  - `const api::tarpit::HONEYPOT_PATHS: &[&str]`.

- [ ] **Step 1: Añadir la dependencia `async-stream` a `backend/crates/api/Cargo.toml`**

En la sección `[dependencies]`, añadir la línea (debajo de `db = { path = "../db" }`):

```toml
async-stream = "0.3"
```

- [ ] **Step 2: Escribir `backend/crates/api/src/tarpit.rs` con los tests del gating que fallan**

```rust
use std::sync::Arc;
use std::time::Duration;

use axum::{
    body::{Body, Bytes},
    extract::State,
    http::StatusCode,
    response::{IntoResponse, Response},
};
use tokio::sync::{OwnedSemaphorePermit, Semaphore};

use crate::AppState;

/// Rutas-cebo: paths que solo tocan escáneres/bots, nunca clientes legítimos.
pub const HONEYPOT_PATHS: &[&str] = &[
    "/.env",
    "/.git/config",
    "/.aws/credentials",
    "/wp-login.php",
    "/wp-admin",
    "/phpmyadmin",
    "/admin.php",
    "/config.php",
    "/server-status",
];

/// Límites duros para que el tarpit NUNCA pueda agotar el origen.
#[derive(Clone, Debug)]
pub struct TarpitConfig {
    pub enabled: bool,
    pub max_concurrent: usize,
    pub drip_interval: Duration,
    pub max_bytes: usize,
}

impl Default for TarpitConfig {
    fn default() -> Self {
        Self {
            enabled: true,
            max_concurrent: 256,
            drip_interval: Duration::from_millis(500),
            max_bytes: 512,
        }
    }
}

impl TarpitConfig {
    pub fn from_env() -> Self {
        let enabled = std::env::var("TARPIT_ENABLED")
            .map(|v| v != "false" && v != "0")
            .unwrap_or(true);
        Self { enabled, ..Self::default() }
    }
}

/// Resultado de intentar atrapar una conexión.
pub enum TarpitDecision {
    /// Atrapar: el permiso vive mientras dure el goteo; al soltarse libera capacidad.
    Trap(OwnedSemaphorePermit),
    /// No atrapar (deshabilitado o tope lleno) → respuesta barata.
    Decline,
}

#[derive(Clone)]
pub struct Tarpit {
    pub config: TarpitConfig,
    sem: Arc<Semaphore>,
}

impl Tarpit {
    pub fn new(config: TarpitConfig) -> Self {
        let sem = Arc::new(Semaphore::new(config.max_concurrent));
        Self { config, sem }
    }

    /// Permisos libres (métricas/tests).
    pub fn available(&self) -> usize {
        self.sem.available_permits()
    }

    /// Decide si atrapar. Si el tope está lleno, declina en lugar de
    /// consumir más recursos del origen.
    pub fn try_trap(&self) -> TarpitDecision {
        if !self.config.enabled {
            return TarpitDecision::Decline;
        }
        match self.sem.clone().try_acquire_owned() {
            Ok(permit) => TarpitDecision::Trap(permit),
            Err(_) => TarpitDecision::Decline,
        }
    }
}

/// Handler de rutas-cebo: gotea bytes lentamente para malgastar el tiempo
/// del bot, con límites estrictos. Si declina, responde 404 barato.
pub async fn handler(State(state): State<AppState>) -> Response {
    match state.tarpit.try_trap() {
        TarpitDecision::Decline => StatusCode::NOT_FOUND.into_response(),
        TarpitDecision::Trap(permit) => {
            let interval = state.tarpit.config.drip_interval;
            let max_bytes = state.tarpit.config.max_bytes;
            let stream = async_stream::stream! {
                let _permit = permit; // liberado al terminar o desconectar
                for _ in 0..max_bytes {
                    tokio::time::sleep(interval).await;
                    yield Ok::<Bytes, std::io::Error>(Bytes::from_static(b"."));
                }
            };
            Response::builder()
                .status(StatusCode::OK)
                .header("content-type", "text/plain; charset=utf-8")
                .body(Body::from_stream(stream))
                .expect("valid tarpit response")
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn cfg(enabled: bool, max_concurrent: usize) -> TarpitConfig {
        TarpitConfig { enabled, max_concurrent, ..TarpitConfig::default() }
    }

    #[test]
    fn declines_when_disabled() {
        let t = Tarpit::new(cfg(false, 8));
        assert!(matches!(t.try_trap(), TarpitDecision::Decline));
    }

    #[test]
    fn caps_concurrent_traps_to_protect_origin() {
        let t = Tarpit::new(cfg(true, 2));

        let a = t.try_trap();
        let b = t.try_trap();
        assert!(matches!(a, TarpitDecision::Trap(_)));
        assert!(matches!(b, TarpitDecision::Trap(_)));
        assert_eq!(t.available(), 0);

        // Tope lleno → declina (no consume más recursos del origen).
        assert!(matches!(t.try_trap(), TarpitDecision::Decline));

        // Al soltar un permiso, vuelve a haber capacidad.
        drop(a);
        assert_eq!(t.available(), 1);
        assert!(matches!(t.try_trap(), TarpitDecision::Trap(_)));

        drop(b);
    }
}
```

- [ ] **Step 3: Correr los tests para confirmar que fallan**

Run: `cd backend && cargo test -p api tarpit`
Expected: FALLA en compilación — `crate::AppState` aún no tiene el campo `tarpit` ni `lib.rs` declara `pub mod tarpit;` (se arregla en Step 5).

- [ ] **Step 4: Verificar la lógica de gating de forma aislada**

> Los tests no dependen del enrutado ni del goteo (que es temporal y propenso a flakiness); validan el **invariante de seguridad**: el semáforo limita las conexiones atrapadas y declina al llenarse.

- [ ] **Step 5: Cablear el tarpit en `backend/crates/api/src/lib.rs`**

Reemplazar el contenido completo del archivo por:

```rust
pub mod config;
pub mod health;
pub mod tarpit;

use axum::{
    routing::{any, get},
    Router,
};
use db::Pool;
use tarpit::{Tarpit, TarpitConfig};

#[derive(Clone)]
pub struct AppState {
    pub pool: Pool,
    pub tarpit: Tarpit,
}

pub fn app(pool: Pool) -> Router {
    let state = AppState {
        pool,
        tarpit: Tarpit::new(TarpitConfig::from_env()),
    };

    let mut router = Router::new().route("/health", get(health::health));
    for path in tarpit::HONEYPOT_PATHS {
        router = router.route(path, any(tarpit::handler));
    }
    router.with_state(state)
}
```

- [ ] **Step 6: Correr los tests del crate para confirmar que pasan**

Run: `cd backend && cargo test -p api`
Expected: PASA — `declines_when_disabled ... ok`, `caps_concurrent_traps_to_protect_origin ... ok`, y el test de integración `health_reports_ok_and_db_up ... ok` sigue verde (el campo nuevo de `AppState` se construye dentro de `app()`, no rompe el test).

- [ ] **Step 7: Smoke test manual (drip lento vs. ruta legítima rápida)**

Run (con Docker Compose arriba y el binario corriendo):
```bash
# Ruta legítima: instantánea
curl -s -w "\n%{time_total}s\n" http://localhost:8080/health
# Ruta-cebo: gotea puntos lentamente (Ctrl+C para cortar)
curl -s http://localhost:8080/.env
```
Expected: `/health` responde en milisegundos con `{"status":"ok","db":"up"}`; `/.env` va imprimiendo `.` lentamente (un punto cada ~500ms) hasta el tope.

- [ ] **Step 8: Formato y lint**

Run: `cd backend && cargo fmt --all && cargo clippy --all-targets -- -D warnings`
Expected: sin cambios de formato pendientes y clippy sin warnings.

- [ ] **Step 9: Commit**

```bash
git add backend/crates/api/src/tarpit.rs backend/crates/api/src/lib.rs backend/crates/api/Cargo.toml
git commit -m "feat(api): HTTP tarpit acotado en rutas-cebo (semáforo + kill-switch)"
```

---

### Task 7: Dockerfile del binario `api`

**Files:**
- Create: `backend/Dockerfile`, `backend/.dockerignore`

**Interfaces:**
- Consumes: el workspace Rust (Tasks 3–6).
- Produces: imagen Docker que ejecuta el binario `api` en el puerto 8080.

- [ ] **Step 1: Crear `backend/.dockerignore`**

```dockerignore
target
**/target
.git
```

- [ ] **Step 2: Crear `backend/Dockerfile` (multi-stage)**

```dockerfile
FROM rust:1.82-slim AS builder
WORKDIR /app
COPY . .
RUN cargo build --release --bin api

FROM debian:bookworm-slim AS runtime
RUN apt-get update \
    && apt-get install -y --no-install-recommends ca-certificates \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY --from=builder /app/target/release/api /usr/local/bin/api
ENV BIND_ADDR=0.0.0.0:8080
EXPOSE 8080
CMD ["api"]
```

> Nota: usamos TLS vía `rustls` (feature `tls-rustls` en sqlx), por eso no se instala `libssl`. `ca-certificates` es necesario para validar el certificado TLS de Neon en producción.

- [ ] **Step 3: Construir la imagen**

Run: `docker build -f backend/Dockerfile -t proyecto-x-api ./backend`
Expected: build termina con `naming to docker.io/library/proyecto-x-api` (sin errores de compilación).

- [ ] **Step 4: Ejecutar el contenedor contra la DB local y verificar**

Run (Linux/host con la red de compose):
```bash
docker run --rm -e DATABASE_URL=postgres://dev:dev@host.docker.internal:5432/appdb -p 8080:8080 proyecto-x-api
```
En otra terminal: `curl -s http://localhost:8080/health`
Expected: `{"status":"ok","db":"up"}`. Detener con Ctrl+C.

- [ ] **Step 5: Commit**

```bash
git add backend/Dockerfile backend/.dockerignore
git commit -m "chore(api): Dockerfile multi-stage para el binario api"
```

---

### Task 8: CI en GitHub Actions

**Files:**
- Create: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: todo el workspace `backend`.
- Produces: pipeline que corre en push/PR: fmt, clippy (`-D warnings`), tests (con Postgres service) y `cargo audit`.

- [ ] **Step 1: Crear `.github/workflows/ci.yml`**

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    services:
      postgres:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_USER: dev
          POSTGRES_PASSWORD: dev
          POSTGRES_DB: appdb
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U dev -d appdb"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      TEST_DATABASE_URL: postgres://dev:dev@localhost:5432/appdb
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: backend
      - name: Format check
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings
      - name: Tests
        run: cargo test --all
      - name: Security audit
        run: |
          cargo install cargo-audit --locked
          cargo audit
```

- [ ] **Step 2: Validar el YAML localmente**

Run: `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('yaml ok')"`
Expected: `yaml ok` (sin excepción de parseo).

- [ ] **Step 3: Commit y push para disparar CI**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: pipeline backend (fmt, clippy, tests con Postgres, audit)"
```
> El push al remoto y la verificación del run verde se hacen cuando el repo tenga remoto configurado (se decide con el usuario; el plan no fuerza push).

- [ ] **Step 4: Verificación local equivalente al CI**

Run:
```bash
cd backend && cargo fmt --all -- --check && cargo clippy --all-targets -- -D warnings && cargo test --all
```
Expected: los tres pasos terminan sin error (equivalente a lo que correrá el CI, salvo `cargo audit`).

---

## Self-Review

**1. Spec coverage (F0.1 dentro de "F0 — Fundaciones"):**
- Monorepo skeleton (estructura pnpm/turbo + carpetas) → Task 1. ✅
- Workspace Rust (`api` + lib `db`) → Tasks 3–5. ✅ (`realtime`/`worker`/`domain` se difieren a sus fases; YAGNI hasta tener lógica que los justifique — se anota en el spec roadmap.)
- Neon/Postgres + PostGIS disponible local → Task 2 (imagen `postgis`). El **esquema y migraciones** son F0.2 (fuera de alcance aquí, declarado en Global Constraints). ✅
- CI con security baseline (`cargo audit`, clippy `-D warnings`) → Task 8. ✅
- HTTP Tarpit acotado (honeypot) → Task 6 (incluido por decisión del equipo; semáforo con tope, kill-switch y límites de bytes/tiempo). ✅
- Docker para despliegue en VPS → Task 7. ✅
- Redis local disponible → Task 2 (se *consume* en F0.2+). ✅
- Auth, RN shell, Next.js, Terraform, observabilidad → **fuera de F0.1** (sub-planes F0.3–F0.7). Sin gaps no declarados.

**2. Placeholder scan:** sin `TBD`/`TODO`/"add error handling"/"write tests for the above". Todo paso con código muestra el código completo. ✅

**3. Type consistency:**
- `db::Pool`, `db::connect`, `db::ping` definidos en Task 3 y consumidos con esas firmas exactas en Tasks 5 y en los tests. ✅
- `api::app(pool: db::Pool) -> Router` y `api::AppState { pool }` definidos en Task 5 y usados con esa firma en el test de Task 5. ✅
- `Config::from_env`/`from_getter` definidos en Task 4 y usados en `main.rs` (Task 5). ✅
- `lib.rs` se crea mínimo en Task 4 (solo `pub mod config;`) y se **reemplaza completo** en Task 5 (Step 4 lo indica explícitamente) → sin colisión de contenido. ✅

- `AppState` gana el campo `tarpit` en Task 6, pero `api::app(pool)` lo construye internamente; el test de Task 5 llama a `app(pool)` sin cambios → no se rompe. `api::tarpit::{Tarpit, TarpitConfig, TarpitDecision, handler, HONEYPOT_PATHS}` se definen en Task 6 y se usan con esas firmas en `lib.rs`. ✅

Sin issues pendientes.

---

## Execution Handoff

Plan completo y guardado en `docs/superpowers/plans/2026-06-25-f0.1-monorepo-backend-scaffold.md`.
