# F0.2 — Esquema completo de Neon + migraciones — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Asentar el modelo de datos completo (§6 del spec maestro) como migraciones sqlx versionadas, con `db::migrate()` + subcomando `api migrate`, repos núcleo con queries verificadas en compilación (`query!` + cache `.sqlx`), y CI en offline mode.

**Architecture:** 8 migraciones SQL agrupadas por dominio en `backend/migrations/`. El binario `api` gana un subcomando `migrate`. La lib `db` expone `migrate()` y repos núcleo (`users`, `auth_identities`, `consent_records`). Tests aplican migraciones contra el Postgres local (host 5433) y validan esquema + round-trips.

**Tech Stack:** Rust (sqlx 0.8 con `migrate` + `macros`), PostgreSQL 16 + PostGIS, sqlx-cli, GitHub Actions.

## Global Constraints

Del spec maestro y del spec F0.2 (`docs/superpowers/specs/2026-06-26-f0.2-esquema-completo-design.md`). Aplica a todas las tareas:

- **DB:** Neon (PostgreSQL + PostGIS) fuente de verdad. Acceso vía **sqlx**. A partir de aquí se usan **queries verificadas en compilación** (`sqlx::query!`/`query_as!`) con cache **`.sqlx/`** commiteado; `SQLX_OFFLINE=true` en build/CI.
- **Migraciones:** archivos SQL versionados en `backend/migrations/NNNN_*.sql` (solo "up"); se aplican con `sqlx::migrate!` vía subcomando, **no** automáticamente en cada arranque.
- **Modelado:** PK `uuid` (`gen_random_uuid()`); `timestamptz` + trigger `set_updated_at()`; conjuntos cerrados como `text` + `CHECK`; geo `geography(Point,4326)` + GiST; FKs `ON DELETE CASCADE` en datos del usuario (purga GDPR); `users.deleted_at` soft-delete.
- **Entorno local (XAMPP):** Postgres dockerizado en **host 5433** (contenedor 5432). `TEST_DATABASE_URL` por defecto `postgres://dev:dev@localhost:5433/appdb`. No usar 5432 (Postgres nativo) ni 8080 (Apache).
- **Herramientas de archivo:** crear/editar con Write/Edit (UTF-8), nunca redirección de PowerShell.
- **Seguridad:** `cargo audit` sigue como gate (con el ignore acotado existente); `clippy -D warnings`.
- **Commits:** uno por tarea; mensaje termina con `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Sin push.
- **Versiones piso:** Rust stable; sqlx-cli compatible con sqlx 0.8.

## Prerequisites

- Docker Compose arriba (Postgres+PostGIS en 5433, Redis). Verifica: `docker compose -f infra/docker-compose.yml ps`.
- Instalar sqlx-cli (Task 1): `cargo install sqlx-cli --no-default-features --features native-tls,postgres` (o `rustls`).
- `DATABASE_URL` para sqlx-cli/prepare en local: `postgres://dev:dev@localhost:5433/appdb`.

---

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `backend/migrations/0001_extensions.sql` | Extensiones + `set_updated_at()` |
| `backend/migrations/0002_identity.sql` | users, auth_identities, devices, refresh_tokens, consent_records, data_requests |
| `backend/migrations/0003_profiles.sql` | profiles + N:M + social_links + photos + verifications |
| `backend/migrations/0004_geo.sql` | locations, safety_zones (PostGIS) |
| `backend/migrations/0005_social_messaging.sql` | taps, favorites, blocks, profile_views, conversations, conversation_members, messages |
| `backend/migrations/0006_albums.sql` | albums, album_photos, album_shares |
| `backend/migrations/0007_monetization.sql` | subscriptions, entitlements |
| `backend/migrations/0008_trust_safety.sql` | reports, moderation_actions, csam_hits, audit_log |
| `backend/crates/db/src/lib.rs` | + `migrate(pool)` |
| `backend/crates/db/src/users.rs` | repos núcleo (users/auth/consent) con `query!` |
| `backend/crates/db/tests/migrations.rs` | test: migraciones idempotentes + aserciones de esquema |
| `backend/crates/db/tests/repos.rs` | test: round-trips de repos + PostGIS |
| `backend/crates/api/src/main.rs` | + subcomando `migrate` |
| `backend/.sqlx/` | cache de `cargo sqlx prepare` (commiteado) |
| `.github/workflows/ci.yml` | + `SQLX_OFFLINE` + `sqlx prepare --check` |

**Aislamiento de tests (decisión):** los tests de F0.2 usan una **base de datos de test dedicada por ejecución**: crean `appdb_test_<pid>` (vía `CREATE DATABASE`), aplican migraciones ahí, y la dropean al final — evita estado compartido con la DB de desarrollo. Helper común en `tests/common/mod.rs`.

---

### Task 1: Sistema de migraciones + 0001 extensiones + `migrate()` + subcomando

**Files:**
- Create: `backend/migrations/0001_extensions.sql`, `backend/crates/db/tests/common/mod.rs`, `backend/crates/db/tests/migrations.rs`
- Modify: `backend/crates/db/Cargo.toml` (dev-deps si hace falta), `backend/crates/db/src/lib.rs` (añadir `migrate`), `backend/crates/api/src/main.rs` (subcomando `migrate`)

**Interfaces:**
- Consumes: `db::{connect, Pool}`.
- Produces: `async fn db::migrate(pool: &Pool) -> anyhow::Result<()>`; helper de tests `setup_test_db() -> (Pool, String)` (pool a una DB temporal migrada + su nombre); subcomando `api migrate`.

- [ ] **Step 1: Instalar sqlx-cli y crear la migración 0001**

Run: `cargo install sqlx-cli --no-default-features --features rustls,postgres` (idempotente si ya está).
Crear `backend/migrations/0001_extensions.sql`:

```sql
CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS pgcrypto;
CREATE EXTENSION IF NOT EXISTS citext;

CREATE OR REPLACE FUNCTION set_updated_at() RETURNS trigger AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

- [ ] **Step 2: Implementar `db::migrate()`**

En `backend/crates/db/src/lib.rs`, añadir:

```rust
/// Aplica todas las migraciones embebidas (carpeta backend/migrations).
pub async fn migrate(pool: &Pool) -> anyhow::Result<()> {
    sqlx::migrate!("../../migrations").run(pool).await?;
    Ok(())
}
```

> Nota: la ruta `"../../migrations"` es relativa al `Cargo.toml` del crate `db` (`backend/crates/db`), apuntando a `backend/migrations`.

- [ ] **Step 3: Helper de tests `backend/crates/db/tests/common/mod.rs`**

```rust
use sqlx::{Connection, Executor, PgConnection};
use db::Pool;

fn base_url() -> String {
    std::env::var("TEST_DATABASE_URL")
        .unwrap_or_else(|_| "postgres://dev:dev@localhost:5433/appdb".to_string())
}

fn with_db(url: &str, db: &str) -> String {
    // reemplaza el nombre de DB final de la URL
    let (head, _) = url.rsplit_once('/').unwrap();
    format!("{head}/{db}")
}

// Contador para nombres únicos por test (los tests de un binario corren en paralelo).
static COUNTER: std::sync::atomic::AtomicU32 = std::sync::atomic::AtomicU32::new(0);

/// Crea una DB temporal, aplica migraciones, y devuelve (pool, nombre_db).
pub async fn setup_test_db() -> (Pool, String) {
    let base = base_url();
    let n = COUNTER.fetch_add(1, std::sync::atomic::Ordering::SeqCst);
    let name = format!("appdb_test_{}_{}", std::process::id(), n);
    let mut admin = PgConnection::connect(&base).await.expect("admin connect");
    let _ = admin.execute(format!("DROP DATABASE IF EXISTS {name} WITH (FORCE)").as_str()).await;
    admin.execute(format!("CREATE DATABASE {name}").as_str()).await.expect("create db");
    let url = with_db(&base, &name);
    let pool = db::connect(&url).await.expect("connect test db");
    db::migrate(&pool).await.expect("migrate");
    (pool, name)
}

/// Dropea la DB temporal (llamar al final del test).
pub async fn teardown_test_db(pool: Pool, name: &str) {
    pool.close().await;
    let base = base_url();
    let mut admin = PgConnection::connect(&base).await.expect("admin connect");
    let _ = admin.execute(format!("DROP DATABASE IF EXISTS {name} WITH (FORCE)").as_str()).await;
}
```

Asegurar en `backend/crates/db/Cargo.toml` que `[dev-dependencies]` incluye `sqlx` con feature para `PgConnection` (ya disponible vía la dep normal) y `tokio`.

- [ ] **Step 4: Test de migración (RED) `backend/crates/db/tests/migrations.rs`**

```rust
mod common;
use common::{setup_test_db, teardown_test_db};

#[tokio::test]
async fn migrations_apply_and_are_idempotent() {
    let (pool, name) = setup_test_db().await;
    // re-aplicar no debe fallar
    db::migrate(&pool).await.expect("re-migrate idempotent");
    // extensión postgis presente
    let has_postgis: bool =
        sqlx::query_scalar("SELECT EXISTS (SELECT 1 FROM pg_extension WHERE extname='postgis')")
            .fetch_one(&pool).await.unwrap();
    assert!(has_postgis, "postgis extension should exist");
    teardown_test_db(pool, &name).await;
}
```

Run: `cd backend && cargo test -p db --test migrations`
Expected: FALLA al compilar/ejecutar — `db::migrate` no existe aún (si Step 2 no se aplicó) o no hay migraciones; tras Steps 1–3 debe compilar. Si ya compila, el fallo esperado inicial es la ausencia de `migrate`.

- [ ] **Step 5: Subcomando `migrate` en `backend/crates/api/src/main.rs`**

Reemplazar el `main` para soportar `api migrate`:

```rust
use api::config::Config;

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    tracing_subscriber::fmt()
        .with_env_filter(
            tracing_subscriber::EnvFilter::try_from_default_env()
                .unwrap_or_else(|_| "info".into()),
        )
        .init();

    let config = Config::from_env()?;
    let pool = db::connect(&config.database_url).await?;

    // Subcomando: `api migrate` aplica migraciones y termina.
    if std::env::args().nth(1).as_deref() == Some("migrate") {
        db::migrate(&pool).await?;
        tracing::info!("migrations applied");
        return Ok(());
    }

    let app = api::app(pool);
    let listener = tokio::net::TcpListener::bind(&config.bind_addr).await?;
    tracing::info!("api listening on {}", config.bind_addr);
    axum::serve(listener, app).await?;
    Ok(())
}
```

- [ ] **Step 6: Test en verde**

Run: `cd backend && cargo test -p db --test migrations`
Expected: PASA — `migrations_apply_and_are_idempotent ... ok` (requiere Postgres en 5433; el test crea/dropa su DB temporal).

- [ ] **Step 7: fmt + clippy + commit**

Run: `cd backend && cargo fmt --all && cargo clippy --all-targets -- -D warnings`
```bash
git add backend/migrations/0001_extensions.sql backend/crates/db backend/crates/api/src/main.rs
git commit -m "feat(db): sistema de migraciones sqlx + extensiones + subcomando migrate

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Migración 0002 — identidad/cuenta

**Files:**
- Create: `backend/migrations/0002_identity.sql`
- Modify: `backend/crates/db/tests/migrations.rs` (aserciones de identidad)

**Interfaces:**
- Consumes: el sistema de migraciones (Task 1).
- Produces: tablas `users`, `auth_identities`, `devices`, `refresh_tokens`, `consent_records`, `data_requests`.

- [ ] **Step 1: Crear `backend/migrations/0002_identity.sql`**

```sql
CREATE TABLE users (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  email citext NOT NULL UNIQUE,
  phone text UNIQUE,
  password_hash text,
  email_verified boolean NOT NULL DEFAULT false,
  phone_verified boolean NOT NULL DEFAULT false,
  dob date,
  age_verified boolean NOT NULL DEFAULT false,
  status text NOT NULL DEFAULT 'active' CHECK (status IN ('active','suspended','banned','deleted')),
  role text NOT NULL DEFAULT 'user' CHECK (role IN ('user','moderator','admin')),
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  deleted_at timestamptz
);
CREATE TRIGGER users_set_updated_at BEFORE UPDATE ON users
  FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TABLE auth_identities (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  provider text NOT NULL CHECK (provider IN ('apple','google','password')),
  provider_uid text NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE (provider, provider_uid)
);
CREATE INDEX idx_auth_identities_user ON auth_identities(user_id);

CREATE TABLE devices (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  platform text CHECK (platform IN ('ios','android','web')),
  push_token text,
  last_seen_at timestamptz,
  created_at timestamptz NOT NULL DEFAULT now(),
  UNIQUE (user_id, push_token)
);
CREATE INDEX idx_devices_user ON devices(user_id);

CREATE TABLE refresh_tokens (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  token_hash text NOT NULL UNIQUE,
  device_id uuid REFERENCES devices(id) ON DELETE SET NULL,
  issued_at timestamptz NOT NULL DEFAULT now(),
  expires_at timestamptz NOT NULL,
  revoked_at timestamptz
);
CREATE INDEX idx_refresh_tokens_user ON refresh_tokens(user_id);

CREATE TABLE consent_records (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  kind text NOT NULL CHECK (kind IN ('tos','privacy','age','marketing')),
  version text NOT NULL,
  granted_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_consent_user ON consent_records(user_id);

CREATE TABLE data_requests (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  kind text NOT NULL CHECK (kind IN ('export','delete')),
  status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','processing','completed','rejected')),
  requested_at timestamptz NOT NULL DEFAULT now(),
  completed_at timestamptz
);
```

- [ ] **Step 2: Añadir aserción a `tests/migrations.rs`**

Añadir un test:

```rust
#[tokio::test]
async fn identity_tables_exist() {
    let (pool, name) = setup_test_db().await;
    for table in ["users","auth_identities","devices","refresh_tokens","consent_records","data_requests"] {
        let exists: bool = sqlx::query_scalar(
            "SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name=$1)")
            .bind(table).fetch_one(&pool).await.unwrap();
        assert!(exists, "table {table} should exist");
    }
    teardown_test_db(pool, &name).await;
}
```

- [ ] **Step 3: Ejecutar y commitear**

Run: `cd backend && cargo test -p db --test migrations`
Expected: PASA (ambos tests). Luego fmt+clippy y:
```bash
git add backend/migrations/0002_identity.sql backend/crates/db/tests/migrations.rs
git commit -m "feat(db): migración 0002 identidad/cuenta

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Migraciones 0003 (perfiles) + 0004 (geo)

**Files:**
- Create: `backend/migrations/0003_profiles.sql`, `backend/migrations/0004_geo.sql`
- Modify: `backend/crates/db/tests/migrations.rs` (aserciones + test PostGIS), `backend/crates/db/tests/repos.rs` (PostGIS round-trip)

**Interfaces:**
- Consumes: 0002 (users).
- Produces: tablas de perfil + geo.

- [ ] **Step 1: `backend/migrations/0003_profiles.sql`**

```sql
CREATE TABLE profiles (
  user_id uuid PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  display_name text,
  about text,
  position text CHECK (position IN ('top','vers_top','versatile','vers_bottom','bottom','side')),
  body_type text,
  height_cm int,
  weight_kg int,
  relationship_status text,
  gender_identity text,
  pronouns text,
  hiv_status text,
  last_tested_on date,
  prep boolean,
  accept_nsfw boolean NOT NULL DEFAULT false,
  show_age boolean NOT NULL DEFAULT true,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);
CREATE TRIGGER profiles_set_updated_at BEFORE UPDATE ON profiles
  FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TABLE profile_tribes (
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  tribe text NOT NULL,
  PRIMARY KEY (user_id, tribe)
);
CREATE TABLE profile_looking_for (
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  intent text NOT NULL,
  PRIMARY KEY (user_id, intent)
);
CREATE TABLE profile_meet_at (
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  place text NOT NULL,
  PRIMARY KEY (user_id, place)
);
CREATE TABLE profile_tags (
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  tag citext NOT NULL,
  PRIMARY KEY (user_id, tag)
);
CREATE TABLE profile_ethnicities (
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  ethnicity text NOT NULL,
  PRIMARY KEY (user_id, ethnicity)
);

CREATE TABLE social_links (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  kind text NOT NULL,
  value text NOT NULL
);
CREATE INDEX idx_social_links_user ON social_links(user_id);

CREATE TABLE photos (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  r2_key text NOT NULL,
  blur_key text,
  position int NOT NULL DEFAULT 0,
  is_primary boolean NOT NULL DEFAULT false,
  is_nsfw boolean NOT NULL DEFAULT false,
  moderation_status text NOT NULL DEFAULT 'pending' CHECK (moderation_status IN ('pending','approved','rejected')),
  width int,
  height int,
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_photos_user_pos ON photos(user_id, position);

CREATE TABLE verifications (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  kind text NOT NULL CHECK (kind IN ('photo','id')),
  status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','approved','rejected')),
  verified_at timestamptz,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

- [ ] **Step 2: `backend/migrations/0004_geo.sql`**

```sql
CREATE TABLE locations (
  user_id uuid PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  geog geography(Point,4326) NOT NULL,
  accuracy_m double precision,
  show_distance boolean NOT NULL DEFAULT true,
  is_incognito boolean NOT NULL DEFAULT false,
  roam_geog geography(Point,4326),
  updated_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_locations_geog ON locations USING GIST (geog);
CREATE INDEX idx_locations_roam ON locations USING GIST (roam_geog);
CREATE TRIGGER locations_set_updated_at BEFORE UPDATE ON locations
  FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TABLE safety_zones (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  geog geography(Point,4326) NOT NULL,
  radius_m double precision NOT NULL,
  action text NOT NULL CHECK (action IN ('hide_distance','hide_profile')),
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_safety_zones_geog ON safety_zones USING GIST (geog);
CREATE INDEX idx_safety_zones_user ON safety_zones(user_id);
```

- [ ] **Step 3: Aserciones + PostGIS round-trip**

Añadir a `tests/migrations.rs` una aserción de que `profiles`, `photos`, `locations`, `safety_zones` existen (mismo patrón que Task 2).

Crear `backend/crates/db/tests/repos.rs` con un test PostGIS:

```rust
mod common;
use common::{setup_test_db, teardown_test_db};
use sqlx::Row;

#[tokio::test]
async fn postgis_location_within_distance() {
    let (pool, name) = setup_test_db().await;
    // un usuario
    let uid: uuid::Uuid = sqlx::query_scalar(
        "INSERT INTO users (email) VALUES ('geo@test.com') RETURNING id")
        .fetch_one(&pool).await.unwrap();
    // su ubicación (lon, lat)
    sqlx::query("INSERT INTO locations (user_id, geog) VALUES ($1, ST_SetSRID(ST_MakePoint($2,$3),4326)::geography)")
        .bind(uid).bind(-3.7038_f64).bind(40.4168_f64) // Madrid
        .execute(&pool).await.unwrap();
    // buscar dentro de 1km de un punto a ~100m
    let count: i64 = sqlx::query_scalar(
        "SELECT count(*) FROM locations WHERE ST_DWithin(geog, ST_SetSRID(ST_MakePoint($1,$2),4326)::geography, 1000)")
        .bind(-3.7040_f64).bind(40.4170_f64)
        .fetch_one(&pool).await.unwrap();
    assert_eq!(count, 1, "location should be within 1km");
    teardown_test_db(pool, &name).await;
}
```

Añadir `uuid = { version = "1", features = ["v4"] }` a `[dev-dependencies]` de `db` (y a `[dependencies]` si los repos lo necesitan en Task 6).

- [ ] **Step 4: Ejecutar y commitear**

Run: `cd backend && cargo test -p db`
Expected: PASA (migraciones + PostGIS). fmt+clippy y commit:
```bash
git add backend/migrations/0003_profiles.sql backend/migrations/0004_geo.sql backend/crates/db
git commit -m "feat(db): migraciones 0003 perfiles + 0004 geo (PostGIS)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Migraciones 0005 (social/mensajería) + 0006 (álbumes)

**Files:**
- Create: `backend/migrations/0005_social_messaging.sql`, `backend/migrations/0006_albums.sql`
- Modify: `backend/crates/db/tests/migrations.rs` (aserciones)

**Interfaces:**
- Consumes: 0002 (users), 0003 (photos).
- Produces: tablas sociales/mensajería + álbumes.

- [ ] **Step 1: `backend/migrations/0005_social_messaging.sql`**

```sql
CREATE TABLE taps (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  from_user uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  to_user uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  type text NOT NULL CHECK (type IN ('looking','hot','friendly')),
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_taps_to ON taps(to_user, created_at);

CREATE TABLE favorites (
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  target_id uuid REFERENCES users(id) ON DELETE CASCADE,
  created_at timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (user_id, target_id)
);

CREATE TABLE blocks (
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  target_id uuid REFERENCES users(id) ON DELETE CASCADE,
  created_at timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (user_id, target_id)
);

CREATE TABLE profile_views (
  viewer_id uuid REFERENCES users(id) ON DELETE CASCADE,
  target_id uuid REFERENCES users(id) ON DELETE CASCADE,
  viewed_at timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (viewer_id, target_id)
);
CREATE INDEX idx_profile_views_target ON profile_views(target_id, viewed_at);

CREATE TABLE conversations (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  created_at timestamptz NOT NULL DEFAULT now(),
  last_message_at timestamptz
);

CREATE TABLE conversation_members (
  conversation_id uuid REFERENCES conversations(id) ON DELETE CASCADE,
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  last_read_at timestamptz,
  muted boolean NOT NULL DEFAULT false,
  PRIMARY KEY (conversation_id, user_id)
);
CREATE INDEX idx_conv_members_user ON conversation_members(user_id);

CREATE TABLE messages (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  conversation_id uuid NOT NULL REFERENCES conversations(id) ON DELETE CASCADE,
  sender_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  kind text NOT NULL CHECK (kind IN ('text','photo','ephemeral_photo','audio','location','album_share')),
  body text,
  media_key text,
  expires_after_view boolean NOT NULL DEFAULT false,
  view_seconds int,
  viewed_at timestamptz,
  unsent_at timestamptz,
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_messages_conv ON messages(conversation_id, created_at);
```

- [ ] **Step 2: `backend/migrations/0006_albums.sql`**

```sql
CREATE TABLE albums (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name text,
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_albums_owner ON albums(owner_id);

CREATE TABLE album_photos (
  album_id uuid REFERENCES albums(id) ON DELETE CASCADE,
  photo_id uuid REFERENCES photos(id) ON DELETE CASCADE,
  position int NOT NULL DEFAULT 0,
  PRIMARY KEY (album_id, photo_id)
);

CREATE TABLE album_shares (
  album_id uuid REFERENCES albums(id) ON DELETE CASCADE,
  shared_with_user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  granted_at timestamptz NOT NULL DEFAULT now(),
  revoked_at timestamptz,
  expires_at timestamptz,
  PRIMARY KEY (album_id, shared_with_user_id)
);
CREATE INDEX idx_album_shares_user ON album_shares(shared_with_user_id);
```

- [ ] **Step 3: Aserciones + commit**

Añadir aserción de existencia de `taps`,`conversations`,`messages`,`albums`,`album_shares` a `tests/migrations.rs`.
Run: `cd backend && cargo test -p db --test migrations` → PASA. fmt+clippy y:
```bash
git add backend/migrations/0005_social_messaging.sql backend/migrations/0006_albums.sql backend/crates/db/tests/migrations.rs
git commit -m "feat(db): migraciones 0005 social/mensajería + 0006 álbumes

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: Migraciones 0007 (monetización) + 0008 (confianza/seguridad)

**Files:**
- Create: `backend/migrations/0007_monetization.sql`, `backend/migrations/0008_trust_safety.sql`
- Modify: `backend/crates/db/tests/migrations.rs` (aserciones)

**Interfaces:**
- Consumes: 0002 (users).
- Produces: tablas de monetización + confianza/seguridad.

- [ ] **Step 1: `backend/migrations/0007_monetization.sql`**

```sql
CREATE TABLE subscriptions (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  tier text NOT NULL DEFAULT 'free' CHECK (tier IN ('free','xtra','unlimited')),
  store text CHECK (store IN ('appstore','playstore','stripe')),
  revenuecat_id text,
  status text CHECK (status IN ('active','expired','grace','canceled')),
  current_period_end timestamptz,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_subscriptions_user ON subscriptions(user_id);
CREATE TRIGGER subscriptions_set_updated_at BEFORE UPDATE ON subscriptions
  FOR EACH ROW EXECUTE FUNCTION set_updated_at();

CREATE TABLE entitlements (
  user_id uuid REFERENCES users(id) ON DELETE CASCADE,
  feature text NOT NULL,
  enabled boolean NOT NULL DEFAULT true,
  PRIMARY KEY (user_id, feature)
);
```

- [ ] **Step 2: `backend/migrations/0008_trust_safety.sql`**

```sql
CREATE TABLE reports (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  reporter_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  target_user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  target_kind text NOT NULL CHECK (target_kind IN ('profile','photo','message')),
  target_id uuid,
  reason text,
  status text NOT NULL DEFAULT 'open' CHECK (status IN ('open','reviewing','actioned','dismissed')),
  created_at timestamptz NOT NULL DEFAULT now(),
  resolved_at timestamptz
);
CREATE INDEX idx_reports_status ON reports(status);

CREATE TABLE moderation_actions (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  moderator_id uuid REFERENCES users(id) ON DELETE SET NULL,
  target_user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  action text NOT NULL CHECK (action IN ('warn','suspend','ban','clear')),
  note text,
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_moderation_target ON moderation_actions(target_user_id);

CREATE TABLE csam_hits (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  photo_id uuid,
  source text,
  hash text,
  matched_at timestamptz NOT NULL DEFAULT now(),
  reported_to_authority_at timestamptz
);

CREATE TABLE audit_log (
  id bigserial PRIMARY KEY,
  actor_id uuid,
  action text,
  target text,
  metadata jsonb,
  created_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_audit_created ON audit_log(created_at);
```

- [ ] **Step 3: Aserciones + commit**

Añadir aserción de `subscriptions`,`entitlements`,`reports`,`moderation_actions`,`csam_hits`,`audit_log`. 
Run: `cd backend && cargo test -p db --test migrations` → PASA. fmt+clippy y:
```bash
git add backend/migrations/0007_monetization.sql backend/migrations/0008_trust_safety.sql backend/crates/db/tests/migrations.rs
git commit -m "feat(db): migraciones 0007 monetización + 0008 confianza/seguridad

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: Repos núcleo con `query!` + cache `.sqlx`

**Files:**
- Create: `backend/crates/db/src/users.rs`
- Modify: `backend/crates/db/src/lib.rs` (módulo `users` + tipos), `backend/crates/db/Cargo.toml` (deps `uuid`, `time`/`chrono` según necesidad)
- Modify: `backend/crates/db/tests/repos.rs` (round-trips)
- Create: `backend/.sqlx/` (cache de prepare)

**Interfaces:**
- Consumes: esquema 0002.
- Produces:
  - `struct db::users::NewUser { email: String, password_hash: Option<String> }`
  - `async fn db::users::create_user(pool, NewUser) -> anyhow::Result<uuid::Uuid>`
  - `async fn db::users::find_user_by_email(pool, &str) -> anyhow::Result<Option<UserRow>>`
  - `async fn db::users::add_auth_identity(pool, user_id, provider, provider_uid) -> anyhow::Result<()>`
  - `async fn db::users::record_consent(pool, user_id, kind, version) -> anyhow::Result<()>`
  - `struct db::users::UserRow { id, email, email_verified, status, role, created_at }`

- [ ] **Step 1: Dependencias**

En `backend/crates/db/Cargo.toml` `[dependencies]` añadir:
```toml
uuid = { version = "1", features = ["v4", "serde"] }
```
(`sqlx` ya está; usar `sqlx::types::time::OffsetDateTime` o habilitar la feature `time`/`chrono` en sqlx si se exponen timestamps. Para minimizar, `UserRow.created_at` se expone como `time::OffsetDateTime` con la feature `sqlx/time`.)
Añadir feature a la dep workspace de sqlx: `"time"` (en `backend/Cargo.toml` `sqlx.features`).

- [ ] **Step 2: Escribir el test (RED) `backend/crates/db/tests/repos.rs`**

Añadir:
```rust
use db::users::{create_user, find_user_by_email, add_auth_identity, record_consent, NewUser};

#[tokio::test]
async fn user_create_find_roundtrip() {
    let (pool, name) = setup_test_db().await;
    let id = create_user(&pool, NewUser { email: "a@b.com".into(), password_hash: Some("h".into()) }).await.unwrap();
    let found = find_user_by_email(&pool, "a@b.com").await.unwrap().expect("user found");
    assert_eq!(found.id, id);
    assert_eq!(found.email, "a@b.com");
    add_auth_identity(&pool, id, "password", &id.to_string()).await.unwrap();
    record_consent(&pool, id, "tos", "1.0").await.unwrap();
    // email único
    let dup = create_user(&pool, NewUser { email: "a@b.com".into(), password_hash: None }).await;
    assert!(dup.is_err(), "duplicate email should fail");
    teardown_test_db(pool, &name).await;
}
```

Run: `cd backend && cargo test -p db --test repos` → FALLA (módulo `users` no existe).

- [ ] **Step 3: Implementar `backend/crates/db/src/users.rs`**

```rust
use crate::Pool;

#[derive(Debug)]
pub struct NewUser {
    pub email: String,
    pub password_hash: Option<String>,
}

#[derive(Debug)]
pub struct UserRow {
    pub id: uuid::Uuid,
    pub email: String,
    pub email_verified: bool,
    pub status: String,
    pub role: String,
    pub created_at: time::OffsetDateTime,
}

pub async fn create_user(pool: &Pool, new: NewUser) -> anyhow::Result<uuid::Uuid> {
    let id = sqlx::query_scalar!(
        "INSERT INTO users (email, password_hash) VALUES ($1, $2) RETURNING id",
        new.email,
        new.password_hash
    )
    .fetch_one(pool)
    .await?;
    Ok(id)
}

pub async fn find_user_by_email(pool: &Pool, email: &str) -> anyhow::Result<Option<UserRow>> {
    let row = sqlx::query_as!(
        UserRow,
        r#"SELECT id, email::text as "email!", email_verified, status, role, created_at
           FROM users WHERE email = $1"#,
        email
    )
    .fetch_optional(pool)
    .await?;
    Ok(row)
}

pub async fn add_auth_identity(
    pool: &Pool,
    user_id: uuid::Uuid,
    provider: &str,
    provider_uid: &str,
) -> anyhow::Result<()> {
    sqlx::query!(
        "INSERT INTO auth_identities (user_id, provider, provider_uid) VALUES ($1, $2, $3)",
        user_id, provider, provider_uid
    )
    .execute(pool)
    .await?;
    Ok(())
}

pub async fn record_consent(
    pool: &Pool,
    user_id: uuid::Uuid,
    kind: &str,
    version: &str,
) -> anyhow::Result<()> {
    sqlx::query!(
        "INSERT INTO consent_records (user_id, kind, version) VALUES ($1, $2, $3)",
        user_id, kind, version
    )
    .execute(pool)
    .await?;
    Ok(())
}
```

En `backend/crates/db/src/lib.rs` añadir `pub mod users;`.

> Nota `query!`: requiere `DATABASE_URL` apuntando a una DB **ya migrada** en tiempo de compilación, o el cache `.sqlx`. Como `email` es `citext`, se castea a `text` en el `SELECT` con alias `"email!"` (no-null forzado).

- [ ] **Step 4: Generar el cache `.sqlx` y verificar GREEN**

Con la DB de desarrollo migrada (host 5433):
```bash
cd backend
# aplicar migraciones a la DB de desarrollo para que prepare vea el esquema:
DATABASE_URL=postgres://dev:dev@localhost:5433/appdb cargo run --bin api migrate
DATABASE_URL=postgres://dev:dev@localhost:5433/appdb cargo sqlx prepare --workspace
cargo test -p db --test repos
```
Expected: `cargo sqlx prepare` crea `backend/.sqlx/` con los query metadata; el test `user_create_find_roundtrip` PASA.

- [ ] **Step 5: fmt + clippy + commit (incluyendo `.sqlx`)**

```bash
cd backend && cargo fmt --all && cargo clippy --all-targets -- -D warnings
git add backend/crates/db backend/Cargo.toml backend/.sqlx
git commit -m "feat(db): repos núcleo (users/auth/consent) con query! + cache sqlx

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: CI — modo offline de sqlx + verificación de cache

**Files:**
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: `.sqlx/` (Task 6), el service Postgres del CI.
- Produces: CI que compila con `SQLX_OFFLINE` y valida el cache.

- [ ] **Step 1: Editar `.github/workflows/ci.yml`**

Añadir al job `backend` la variable de entorno `SQLX_OFFLINE: "true"` (junto a `TEST_DATABASE_URL`), de modo que `cargo fmt`/`clippy`/`test` compilen `query!` desde el cache sin DB en build:

```yaml
    env:
      TEST_DATABASE_URL: postgres://dev:dev@localhost:5432/appdb
      SQLX_OFFLINE: "true"
```

Añadir, **antes** del step de tests, un step que aplica migraciones y valida el cache, usando una `DATABASE_URL` online (el service Postgres del runner):

```yaml
      - name: Run migrations (compila offline desde .sqlx; migra la DB del service)
        env:
          DATABASE_URL: postgres://dev:dev@localhost:5432/appdb
        run: cargo run --bin api migrate
      - name: Verify sqlx cache is up to date
        env:
          DATABASE_URL: postgres://dev:dev@localhost:5432/appdb
          SQLX_OFFLINE: "false"
        run: |
          cargo install sqlx-cli --no-default-features --features rustls,postgres --locked
          cargo sqlx prepare --workspace --check
```

> El step de tests sigue corriendo con el service Postgres; los tests de `db` crean/dropan su propia DB temporal a partir de `TEST_DATABASE_URL`. El usuario `dev` del service tiene permiso para `CREATE DATABASE` (es superuser por defecto en la imagen).

- [ ] **Step 2: Validar YAML + verificación local equivalente**

Run:
```bash
python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('yaml ok')"
cd backend && SQLX_OFFLINE=true cargo clippy --all-targets -- -D warnings
```
Expected: `yaml ok` y clippy limpio compilando desde el cache `.sqlx` (sin DATABASE_URL).

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: sqlx offline mode + verificación del cache .sqlx

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage (vs spec F0.2):**
- Sistema de migraciones + `migrate()` + subcomando → Task 1. ✅
- `0001..0008` (esquema completo §6) → Tasks 1–5. ✅ (todas las tablas del §2 del spec presentes y agrupadas).
- Repos núcleo con `query!` + `.sqlx` → Task 6. ✅
- CI `SQLX_OFFLINE` + `prepare --check` → Task 7. ✅
- Tests migración idempotente + aserciones + PostGIS + repos → Tasks 1,2,3,6. ✅
- Aislamiento de tests (DB temporal) → Task 1 helper. ✅

**2. Placeholder scan:** SQL y código Rust completos en cada paso; sin TBD/TODO. ✅

**3. Type/orden consistency:**
- Orden de creación respeta FKs: `users`→(auth,devices)→`refresh_tokens`(usa devices); `photos` antes de `album_photos`; `conversations` antes de `conversation_members`/`messages`. ✅
- `db::migrate`, `setup_test_db`/`teardown_test_db`, y firmas de repos (`create_user`/`find_user_by_email`/`add_auth_identity`/`record_consent`, `NewUser`, `UserRow`) usadas consistentemente entre Tasks 1, 3 y 6. ✅
- `query!` necesita DB migrada o cache: Task 6 Step 4 genera el cache antes del commit; CI usa offline. ✅

**4. Riesgos:**
- `cargo sqlx prepare --check` en CI fallará si alguien edita un `query!` sin regenerar `.sqlx` — es el comportamiento deseado (gate).
- El usuario `dev` debe poder `CREATE DATABASE` para los tests; lo es en la imagen `postgis/postgis` (superuser). Si no, el helper caería a crear esquema en vez de DB (alternativa documentada si surge).

---

## Execution Handoff

Plan completo y guardado en `docs/superpowers/plans/2026-06-26-f0.2-esquema-completo.md`.
