# F0.5 — Sitio de marketing Next.js (SEO) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un sitio de marketing Next.js (App Router) en `apps/marketing/` con el núcleo SEO (metadata, JSON-LD, sitemap, robots, OG), una landing pulida + páginas legales/soporte/blog, verificado por `next build` + lint + tests.

**Architecture:** App autocontenida en `apps/marketing/` (npm). Baseline con `create-next-app` (App Router + TS + Tailwind). Páginas server-rendered (SSG). Helpers SEO en `lib/`. Tests con vitest + @testing-library/react.

**Tech Stack:** Next.js (App Router) 15+, TypeScript, Tailwind CSS, vitest, @testing-library/react.

## Global Constraints

Del spec maestro (§15) y el spec F0.5 (`docs/superpowers/specs/2026-06-26-f0.5-nextjs-marketing-design.md`):

- **SEO primero:** páginas SSG (HTML completo para crawlers); Metadata API (title/description/canonical/OG/twitter/robots, `metadataBase`); **JSON-LD** (Organization, WebSite, SoftwareApplication, FAQPage); `sitemap.ts` + `robots.ts`; `next/font` + `next/image`; HTML semántico + a11y.
- **El marketing es `index,follow`** (la app de producto sería `noindex`, vive aparte).
- **Idioma inglés** (i18n es fast-follow). **18+** visible; nada explícito (apto stores/indexación).
- **Sin shadcn/librería UI**: Tailwind a medida.
- **App autocontenida** en `apps/marketing/`, **npm** (su propio `package-lock.json`).
- **"Verde":** `next build` + `next lint` + tests (vitest) en verde.
- **Herramientas de archivo:** Write/Edit (UTF-8). Comandos no interactivos (`--yes`).
- **Commits:** uno por tarea; mensaje termina con `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Sin push.

## Prerequisites

- Node ≥ 20 (hay v24), npm, red (descarga Next.js). El backend NO necesita estar corriendo (sitio estático, sin llamadas a la API en build).
- `.gitignore` raíz ya ignora `node_modules`, `.next`, `dist` (patrones sin slash → cualquier nivel). Confirmar `apps/marketing/.next`, `out`, `node_modules` no se commitean.

---

## File Structure (apps/marketing)

| Archivo | Responsabilidad |
|---|---|
| `app/layout.tsx` | `<html lang>`, metadata base, fuente, JSON-LD Organization+WebSite |
| `app/page.tsx` | Landing |
| `app/{privacy,terms,support,blog}/page.tsx` | Páginas |
| `app/sitemap.ts`, `app/robots.ts`, `app/not-found.tsx` | SEO/404 |
| `components/*` | Hero, FeatureGrid, SafetyCallout, StoreButtons, SiteHeader, SiteFooter |
| `lib/site.ts`, `lib/seo.ts` | constantes + helpers SEO |
| `next.config.ts`, `tailwind.config.ts`, `tsconfig.json`, `package.json` | config |
| `vitest.config.ts`, `__tests__/*` | tests |

---

### Task 1: Scaffold Next.js (App Router + TS + Tailwind)

**Files:** genera `apps/marketing/` (create-next-app) + limpia el demo.

- [ ] **Step 1: Generar el baseline**

Run (desde el root del repo):
```bash
npx create-next-app@latest apps/marketing --typescript --tailwind --app --eslint --src-dir=false --import-alias "@/*" --use-npm --yes
```
(App Router + TS + Tailwind + ESLint, alias `@/*`, npm.)

- [ ] **Step 2: Limpiar el demo**

Reemplazar `app/page.tsx` por una landing mínima temporal y `app/globals.css` por los directives de Tailwind solamente. Borrar SVGs demo de `public/` que no usemos (next.svg, vercel.svg). Dejar `app/layout.tsx` mínimo (lo ampliamos en T2). Confirmar que no quedan imports rotos.

`app/page.tsx` temporal:
```tsx
export default function Home() {
  return <main className="min-h-screen flex items-center justify-center bg-neutral-950 text-neutral-100">proyecto-X</main>;
}
```

- [ ] **Step 3: Verificar build + lint**

```bash
cd apps/marketing
npm run build
npm run lint
```
Expected: `next build` OK (rutas estáticas generadas); lint OK.

- [ ] **Step 4: Commit**

```bash
git add apps/marketing
git commit -m "feat(marketing): scaffold Next.js (App Router + TS + Tailwind)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Constantes + helpers SEO + layout (metadata + JSON-LD) + vitest

**Files:**
- Create: `lib/site.ts`, `lib/seo.ts`, `vitest.config.ts`, `__tests__/seo.test.ts`
- Modify: `app/layout.tsx`, `package.json`

- [ ] **Step 1: deps de test**

```bash
cd apps/marketing
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react
```

- [ ] **Step 2: `lib/site.ts`**

```ts
export const site = {
  name: "proyecto-X",
  url: "https://proyecto-x.example",
  tagline: "Meet guys nearby. On your terms.",
  description:
    "proyecto-X is a location-based social and dating app for the LGBTQ+ community (18+). Browse a live grid of guys nearby, chat, share private albums, and stay in control of your privacy.",
  stores: {
    appStore: "https://apps.apple.com/app/id000000000",
    googlePlay: "https://play.google.com/store/apps/details?id=com.proyectox.app",
  },
  social: { x: "https://x.com/proyectox", instagram: "https://instagram.com/proyectox" },
} as const;
```

- [ ] **Step 3: `lib/seo.ts`**

```ts
import type { Metadata } from "next";
import { site } from "./site";

export function buildMetadata(opts: {
  title?: string;
  description?: string;
  path?: string;
}): Metadata {
  const title = opts.title ?? site.name;
  const description = opts.description ?? site.description;
  const url = `${site.url}${opts.path ?? "/"}`;
  return {
    title,
    description,
    alternates: { canonical: url },
    openGraph: {
      title,
      description,
      url,
      siteName: site.name,
      type: "website",
      images: [{ url: "/og.png", width: 1200, height: 630, alt: site.name }],
    },
    twitter: { card: "summary_large_image", title, description, images: ["/og.png"] },
    robots: { index: true, follow: true },
  };
}

export function organizationJsonLd() {
  return {
    "@context": "https://schema.org",
    "@type": "Organization",
    name: site.name,
    url: site.url,
    sameAs: [site.social.x, site.social.instagram],
  };
}

export function websiteJsonLd() {
  return {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: site.name,
    url: site.url,
  };
}

export function softwareAppJsonLd() {
  return {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    name: site.name,
    applicationCategory: "SocialNetworkingApplication",
    operatingSystem: "iOS, Android, Web",
    offers: { "@type": "Offer", price: "0", priceCurrency: "USD" },
  };
}

export function faqJsonLd(faqs: { q: string; a: string }[]) {
  return {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    mainEntity: faqs.map((f) => ({
      "@type": "Question",
      name: f.q,
      acceptedAnswer: { "@type": "Answer", text: f.a },
    })),
  };
}
```

- [ ] **Step 4: `app/layout.tsx`**

```tsx
import type { Metadata } from "next";
import "./globals.css";
import { site } from "@/lib/site";
import { buildMetadata, organizationJsonLd, websiteJsonLd } from "@/lib/seo";

export const metadata: Metadata = {
  metadataBase: new URL(site.url),
  ...buildMetadata({ title: `${site.name} — ${site.tagline}`, path: "/" }),
  title: { default: `${site.name} — ${site.tagline}`, template: `%s · ${site.name}` },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="bg-neutral-950 text-neutral-100 antialiased">
        {children}
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(organizationJsonLd()) }}
        />
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(websiteJsonLd()) }}
        />
      </body>
    </html>
  );
}
```

- [ ] **Step 5: `vitest.config.ts` + `__tests__/seo.test.ts`**

`vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: { environment: "jsdom", globals: true },
  resolve: { alias: { "@": new URL(".", import.meta.url).pathname } },
});
```
`__tests__/seo.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { buildMetadata, organizationJsonLd, softwareAppJsonLd } from "@/lib/seo";
import { site } from "@/lib/site";

describe("seo", () => {
  it("buildMetadata sets canonical + OG", () => {
    const m = buildMetadata({ title: "Privacy", path: "/privacy" });
    expect(m.alternates?.canonical).toBe(`${site.url}/privacy`);
    expect(m.openGraph?.title).toBe("Privacy");
    expect(m.robots).toMatchObject({ index: true, follow: true });
  });
  it("Organization JSON-LD has the right type", () => {
    expect(organizationJsonLd()["@type"]).toBe("Organization");
    expect(softwareAppJsonLd()["@type"]).toBe("SoftwareApplication");
  });
});
```
Añadir a `package.json` scripts: `"test": "vitest run"`.

- [ ] **Step 6: Verificar + commit**

Run: `cd apps/marketing && npm run build && npm test`
```bash
git add apps/marketing
git commit -m "feat(marketing): helpers SEO (metadata + JSON-LD) + layout + vitest

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Landing + componentes + SoftwareApplication JSON-LD

**Files:**
- Create: `components/{SiteHeader,SiteFooter,Hero,FeatureGrid,SafetyCallout,StoreButtons}.tsx`, `__tests__/landing.test.tsx`
- Modify: `app/page.tsx`

- [ ] **Step 1: componentes**

`components/StoreButtons.tsx`:
```tsx
import { site } from "@/lib/site";
export function StoreButtons() {
  return (
    <div className="flex flex-wrap gap-3">
      <a href={site.stores.appStore} className="rounded-xl bg-white px-5 py-3 font-semibold text-neutral-950">App Store</a>
      <a href={site.stores.googlePlay} className="rounded-xl border border-neutral-700 px-5 py-3 font-semibold">Google Play</a>
    </div>
  );
}
```
`components/SiteHeader.tsx`:
```tsx
import Link from "next/link";
import { site } from "@/lib/site";
export function SiteHeader() {
  return (
    <header className="mx-auto flex max-w-6xl items-center justify-between p-6">
      <Link href="/" className="text-lg font-bold">{site.name}</Link>
      <nav className="flex gap-6 text-sm text-neutral-300">
        <Link href="/support">Support</Link>
        <Link href="/privacy">Privacy</Link>
        <Link href="/terms">Terms</Link>
      </nav>
    </header>
  );
}
```
`components/SiteFooter.tsx`:
```tsx
import Link from "next/link";
import { site } from "@/lib/site";
export function SiteFooter() {
  return (
    <footer className="mx-auto max-w-6xl border-t border-neutral-800 p-6 text-sm text-neutral-400">
      <p>© {site.name}. 18+ only.</p>
      <nav className="mt-2 flex gap-4">
        <Link href="/privacy">Privacy</Link>
        <Link href="/terms">Terms</Link>
        <Link href="/support">Support</Link>
        <Link href="/blog">Blog</Link>
      </nav>
    </footer>
  );
}
```
`components/Hero.tsx`:
```tsx
import { site } from "@/lib/site";
import { StoreButtons } from "./StoreButtons";
export function Hero() {
  return (
    <section className="mx-auto max-w-6xl px-6 py-20 text-center">
      <p className="mb-3 text-sm font-medium uppercase tracking-widest text-amber-400">18+ · LGBTQ+</p>
      <h1 className="mx-auto max-w-3xl text-4xl font-extrabold tracking-tight sm:text-6xl">{site.tagline}</h1>
      <p className="mx-auto mt-6 max-w-2xl text-lg text-neutral-300">{site.description}</p>
      <div className="mt-8 flex justify-center"><StoreButtons /></div>
    </section>
  );
}
```
`components/FeatureGrid.tsx`:
```tsx
const features = [
  { title: "Live grid", body: "See guys near you, sorted by distance, online now." },
  { title: "Chat", body: "Message, send photos, share your location — in real time." },
  { title: "Private albums", body: "Share private photos with people you choose, and unshare anytime." },
  { title: "Privacy & safety", body: "Hide your distance, go incognito, and stay in control." },
];
export function FeatureGrid() {
  return (
    <section className="mx-auto max-w-6xl px-6 py-16">
      <h2 className="text-center text-3xl font-bold">Everything you need to connect</h2>
      <div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
        {features.map((f) => (
          <div key={f.title} className="rounded-2xl border border-neutral-800 bg-neutral-900 p-6">
            <h3 className="text-lg font-semibold">{f.title}</h3>
            <p className="mt-2 text-neutral-400">{f.body}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
```
`components/SafetyCallout.tsx`:
```tsx
export function SafetyCallout() {
  return (
    <section className="mx-auto max-w-6xl px-6 py-16">
      <div className="rounded-2xl border border-neutral-800 bg-gradient-to-br from-neutral-900 to-neutral-950 p-10 text-center">
        <h2 className="text-3xl font-bold">Your privacy, by design</h2>
        <p className="mx-auto mt-4 max-w-2xl text-neutral-300">
          Location fuzzing, distance hiding, incognito mode, discreet app icon, and moderation built in.
          You decide what you share and with whom.
        </p>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: `app/page.tsx`**

```tsx
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";
import { Hero } from "@/components/Hero";
import { FeatureGrid } from "@/components/FeatureGrid";
import { SafetyCallout } from "@/components/SafetyCallout";
import { softwareAppJsonLd } from "@/lib/seo";

export default function Home() {
  return (
    <>
      <SiteHeader />
      <main>
        <Hero />
        <FeatureGrid />
        <SafetyCallout />
      </main>
      <SiteFooter />
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(softwareAppJsonLd()) }} />
    </>
  );
}
```

- [ ] **Step 3: `__tests__/landing.test.tsx`**

```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import Home from "@/app/page";

describe("landing", () => {
  it("renders the hero tagline and store CTAs", () => {
    render(<Home />);
    expect(screen.getByRole("heading", { level: 1 })).toBeTruthy();
    expect(screen.getByText("App Store")).toBeTruthy();
    expect(screen.getByText("Google Play")).toBeTruthy();
  });
});
```

- [ ] **Step 4: Verificar + commit**

Run: `cd apps/marketing && npm run build && npm test && npm run lint`
```bash
git add apps/marketing
git commit -m "feat(marketing): landing (hero/features/safety) + componentes + SoftwareApplication JSON-LD

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Páginas (privacy/terms/support/blog) + sitemap + robots

**Files:**
- Create: `app/privacy/page.tsx`, `app/terms/page.tsx`, `app/support/page.tsx`, `app/blog/page.tsx`, `app/sitemap.ts`, `app/robots.ts`, `app/not-found.tsx`

- [ ] **Step 1: páginas legales/soporte (con metadata)**

`app/privacy/page.tsx`:
```tsx
import { buildMetadata } from "@/lib/seo";
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";

export const metadata = buildMetadata({ title: "Privacy Policy", path: "/privacy" });

export default function Privacy() {
  return (
    <>
      <SiteHeader />
      <main className="mx-auto max-w-3xl px-6 py-16 prose-invert">
        <h1 className="text-4xl font-bold">Privacy Policy</h1>
        <p className="mt-4 text-neutral-300">We collect the minimum data needed to run the service and protect a vulnerable community. This summary covers what we collect, why, and your rights (GDPR/CCPA).</p>
        <h2 className="mt-8 text-2xl font-semibold">Data we collect</h2>
        <p className="mt-2 text-neutral-300">Account (email, age verification), profile, approximate location (with controls), messages, and device/push tokens.</p>
        <h2 className="mt-8 text-2xl font-semibold">Your rights</h2>
        <p className="mt-2 text-neutral-300">Access, export, and deletion (right to be forgotten). Contact privacy@proyecto-x.example.</p>
      </main>
      <SiteFooter />
    </>
  );
}
```
`app/terms/page.tsx` (misma estructura, título "Terms of Service", contenido placeholder serio: elegibilidad 18+, conducta, terminación, ley aplicable).

`app/support/page.tsx` (con FAQ + JSON-LD):
```tsx
import { buildMetadata, faqJsonLd } from "@/lib/seo";
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";

export const metadata = buildMetadata({ title: "Support", path: "/support" });

const faqs = [
  { q: "How do I create an account?", a: "Download the app, sign up with email or Apple/Google, and verify you're 18+." },
  { q: "How do I stay safe?", a: "Use distance hiding, incognito, blocking and reporting. We moderate content and respond to reports." },
  { q: "How do I delete my account?", a: "Settings → Account → Delete account. Your data is erased per our privacy policy." },
];

export default function Support() {
  return (
    <>
      <SiteHeader />
      <main className="mx-auto max-w-3xl px-6 py-16">
        <h1 className="text-4xl font-bold">Support</h1>
        <div className="mt-8 space-y-6">
          {faqs.map((f) => (
            <div key={f.q}>
              <h2 className="text-xl font-semibold">{f.q}</h2>
              <p className="mt-2 text-neutral-300">{f.a}</p>
            </div>
          ))}
        </div>
      </main>
      <SiteFooter />
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqJsonLd(faqs)) }} />
    </>
  );
}
```
`app/blog/page.tsx` (placeholder):
```tsx
import { buildMetadata } from "@/lib/seo";
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";

export const metadata = buildMetadata({ title: "Blog", path: "/blog" });

export default function Blog() {
  return (
    <>
      <SiteHeader />
      <main className="mx-auto max-w-3xl px-6 py-16">
        <h1 className="text-4xl font-bold">Blog</h1>
        <p className="mt-4 text-neutral-300">Coming soon — guides on safety, dating, and community.</p>
      </main>
      <SiteFooter />
    </>
  );
}
```

- [ ] **Step 2: `app/sitemap.ts` + `app/robots.ts` + `app/not-found.tsx`**

`app/sitemap.ts`:
```ts
import type { MetadataRoute } from "next";
import { site } from "@/lib/site";

export default function sitemap(): MetadataRoute.Sitemap {
  const routes = ["", "/privacy", "/terms", "/support", "/blog"];
  return routes.map((r) => ({ url: `${site.url}${r}`, lastModified: new Date("2026-06-26"), changeFrequency: "weekly", priority: r === "" ? 1 : 0.7 }));
}
```
> Nota: `new Date("2026-06-26")` con argumento es válido (no es `Date.now()`); fija la fecha para builds reproducibles.

`app/robots.ts`:
```ts
import type { MetadataRoute } from "next";
import { site } from "@/lib/site";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: "*", allow: "/" },
    sitemap: `${site.url}/sitemap.xml`,
    host: site.url,
  };
}
```
`app/not-found.tsx`:
```tsx
import Link from "next/link";
export default function NotFound() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4">
      <h1 className="text-3xl font-bold">404</h1>
      <Link href="/" className="text-amber-400">Go home</Link>
    </main>
  );
}
```

- [ ] **Step 3: Verificar + commit**

Run: `cd apps/marketing && npm run build && npm test && npm run lint`
Expected: build genera `/privacy`, `/terms`, `/support`, `/blog`, `/sitemap.xml`, `/robots.txt`.
```bash
git add apps/marketing
git commit -m "feat(marketing): privacy/terms/support(FAQ)/blog + sitemap + robots + 404

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: Cierre — OG image, README, verificación final

**Files:**
- Create: `public/og.png` (placeholder o generado), `apps/marketing/README.md`
- Modify: `package.json` (scripts si faltan)

- [ ] **Step 1: OG image**

Crear un `public/og.png` (1200×630). Si no se puede generar binario, crear un `app/opengraph-image.tsx` que genere la OG con `next/og` (ImageResponse) — preferible (dinámico, sin binario):
```tsx
import { ImageResponse } from "next/og";
import { site } from "@/lib/site";
export const size = { width: 1200, height: 630 };
export const contentType = "image/png";
export default function OG() {
  return new ImageResponse(
    (
      <div style={{ width: "100%", height: "100%", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", background: "#0a0a0a", color: "#fff", fontSize: 64, fontWeight: 800 }}>
        <div>{site.name}</div>
        <div style={{ fontSize: 32, fontWeight: 400, color: "#d4d4d4", marginTop: 16 }}>{site.tagline}</div>
      </div>
    ),
    size,
  );
}
```
(Si se usa `opengraph-image.tsx`, ajustar `lib/seo.ts` para no forzar `/og.png` o dejar ambos; Next sirve la OG generada automáticamente por ruta. Documentar la elección.)

- [ ] **Step 2: README**

`apps/marketing/README.md`:
```markdown
# proyecto-X — marketing (Next.js)

Sitio público de marketing (SEO) en Next.js App Router. Separado de la app de producto (que va `noindex`).

## Desarrollo
- `npm install`
- `npm run dev` (http://localhost:3000)
- `npm run build` · `npm test` · `npm run lint`
- SEO: metadata + JSON-LD (Organization/WebSite/SoftwareApplication/FAQ), `sitemap.xml`, `robots.txt`, OG dinámica.

## Despliegue
- Vercel (cuando se conecte la cuenta): `vercel` / `vercel --prod`.
```

- [ ] **Step 3: Verificación final + commit**

```bash
cd apps/marketing
npm run build && npm run lint && npm test
git add apps/marketing
git commit -m "chore(marketing): OG image + README + verificación

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage:** scaffold (T1); SEO helpers + layout + JSON-LD (T2); landing + componentes (T3); páginas + sitemap + robots (T4); OG + README + cierre (T5). ✅
**2. Placeholder scan:** código completo por archivo; el scaffold delegado a create-next-app (versiones correctas). El contenido legal es placeholder *serio* (no lorem), apropiado para un shell. ✅
**3. Consistencia:** `site`/`buildMetadata`/`*JsonLd` usados consistentemente entre T2/T3/T4. Rutas del sitemap = páginas creadas. ✅
**4. Riesgos (Next tooling):**
- create-next-app trae demo (page.tsx/globals.css) — T1 lo limpia.
- Tailwind v4 vs v3: create-next-app actual usa Tailwind v4 (config en CSS, `@import "tailwindcss"`). Si el `tailwind.config.ts` no existe (v4), no pasa nada; usar las clases directamente. Adaptar si la versión difiere.
- vitest + alias `@`: el `vitest.config.ts` define el alias; si el resolve falla, ajustar al `paths` del tsconfig.
- `opengraph-image.tsx` requiere el runtime de `next/og`; si da problemas en build, caer a un `public/og.png` estático.
- App Router server components en tests: la landing y sus componentes son server components sin async/datos → `@testing-library/react` los renderiza como funciones; si un componente usa APIs server-only, mockear o testear solo los presentacionales.

---

## Execution Handoff

Plan completo y guardado en `docs/superpowers/plans/2026-06-26-f0.5-nextjs-marketing.md`.
