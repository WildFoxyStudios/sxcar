# Spec F0.3 — Autenticación (email/password + OAuth/SMS enchufables)

- **Proyecto:** proyecto-X
- **Fecha:** 2026-06-26
- **Fase:** F0.3 (deriva del §7.1 y §12 del spec maestro)
- **Alcance (elegido por el usuario):** auth **completo** con **proveedores enchufables** — email/password real y testeado sin deps externas; Apple/Google + email/SMS detrás de `trait`s con impl de DEV + impl real tras config.

> Objetivo: un sistema de autenticación de grado producción sobre el esquema F0.2 (`users`, `auth_identities`, `refresh_tokens`, `devices`, `consent_records`), listo para enchufar credenciales reales de Apple/Google/email/SMS sin reescribir.

---

## 1. Arquitectura

- **`backend/crates/auth` (lib):** lógica pura/sin estado de auth.
  - `password.rs` — `hash_password`, `verify_password` (**argon2id**).
  - `jwt.rs` — `AccessClaims { sub, exp, iat }`, `issue_access`, `verify_access` (HS256 vía `jsonwebtoken`; secreto desde config).
  - `tokens.rs` — generación de refresh token opaco (256-bit aleatorio), hashing (SHA-256) para guardar, y verificación. La rotación (revocar viejo, emitir nuevo) se orquesta en el handler.
  - `age.rs` — `is_adult(dob, today) -> bool` (≥18) y `age(dob, today)`.
  - `code.rs` — generación de códigos numéricos de verificación (p. ej. 6 dígitos) + hashing para guardar.
  - `oauth.rs` — `trait OAuthVerifier { async fn verify(provider, id_token) -> Result<OAuthIdentity{provider_uid,email}> }`. Impl real (JWKS de Apple/Google + validación de firma/issuer/audience con client_id de config) **scaffolded**; impl de DEV/test para flujos locales.
  - `notify.rs` — `trait Notifier { async fn send_email(...); async fn send_sms(...) }`. Impl **DEV** (loguea el código). Impls reales (Cloudflare Email / proveedor SMS) tras config — interfaz lista, sin credenciales aún.
  - `error.rs` — `AuthError` con mapeo a códigos HTTP genéricos.
- **`api` handlers** (`backend/crates/api/src/auth/`): endpoints HTTP + extractor `AuthUser` (valida Bearer access token). `AppState` gana: `JwtConfig`, `Arc<dyn Notifier>`, `Arc<dyn OAuthVerifier>`, y un rate-limiter en memoria.
- **`db` repos** (ampliar): `users` (crear con dob/age, set verified, update password), `auth_identities` (find/add), `refresh_tokens` (store/find/revoke), `auth_codes` (issue/find/consume), `consent_records` (record).

## 2. Migración nueva

### `0010_auth_codes.sql`
- **auth_codes**(`id` uuid PK, `user_id` uuid → users CASCADE, `kind` text CHECK in (email_verify, phone_verify, password_reset), `code_hash` text NN, `expires_at` timestamptz NN, `consumed_at` timestamptz, `created_at`). Index(`user_id`,`kind`). Los códigos se guardan **hasheados**; expiran; un solo uso (`consumed_at`).

## 3. Endpoints (todos bajo `/auth`)

| Método/Ruta | Entrada | Acción |
|---|---|---|
| `POST /register` | email, password, dob, consents[] | **age-gate ≥18**; valida; crea user (argon2id); registra consentimientos; emite código `email_verify` y lo envía vía Notifier; devuelve par de tokens (acceso limitado hasta verificar, según política) |
| `POST /login` | email, password | verifica; emite par de tokens; error **genérico** si falla |
| `POST /refresh` | refresh_token | verifica+**rota** (revoca viejo, emite nuevo par) |
| `POST /logout` | refresh_token | revoca el refresh token |
| `POST /verify-email` | code | marca `email_verified` |
| `POST /resend-email` | (auth) | reemite código `email_verify` |
| `POST /send-phone-code` | phone | emite+envía `phone_verify` (SMS vía Notifier) |
| `POST /verify-phone` | code | marca `phone_verified` |
| `POST /password/reset-request` | email | emite `password_reset`; **respuesta siempre 200** (sin enumeración) |
| `POST /password/reset` | code, new_password | valida código; actualiza hash; revoca refresh tokens |
| `POST /oauth/:provider` | id_token (apple\|google) | verifica vía `OAuthVerifier`; find-or-create user + `auth_identities`; emite par |
| **Extractor** `AuthUser` | header `Authorization: Bearer` | valida access JWT → `user_id` para rutas protegidas |

## 4. Seguridad

- **argon2id** (parámetros razonables). **JWT HS256** con secreto desde `JWT_SECRET` (env); access TTL ~15 min, refresh TTL ~30 días **rotatorio**, guardado **hasheado** (SHA-256) en `refresh_tokens`.
- **Sin enumeración de usuarios**: login/reset devuelven errores/200 genéricos; tiempos uniformes en lo posible.
- **Validación de entrada** estricta (`validator`): formato email, fuerza de password mínima, dob válido.
- **Rate-limiting** en endpoints de auth (login, register, reset, oauth): limiter **en memoria** (token bucket por IP+email) para F0.3; nota: Redis distribuido cuando se cablee Redis (fase posterior).
- **Revocación**: logout y password-reset revocan refresh tokens; access tokens caducan rápido.
- Secretos solo por entorno; nunca en repo. Errores no filtran detalles internos.

## 5. Config (añadir a `api::config::Config`)

- `jwt_secret` (env `JWT_SECRET`, requerido), `access_ttl_secs` (default 900), `refresh_ttl_secs` (default 2592000).
- OAuth: `google_client_id`, `apple_client_id` (env, opcionales — si ausentes, el verifier real falla con error claro; en dev se usa el verifier de DEV).
- Selección de provider (`AUTH_NOTIFIER=dev|cloudflare`, `AUTH_OAUTH=dev|real`) — por defecto **dev** en local/test.

## 6. Testing

- **auth lib (unit, sin DB/red):** hash+verify password; issue+verify access JWT (válido/expirado/firma mala); age-gate (17→reject, 18→ok); generación/expiración de códigos; refresh token hashing.
- **Integración (api + DB en 5433, con Notifier+OAuth de DEV):** register→verify-email→login→refresh→logout (flujo completo); register <18 → rechazo; email duplicado → error; login password incorrecto → error genérico; password reset (request→reset→login con nueva); oauth find-or-create (token de DEV) crea user + identity y al repetir reusa; refresh rotado invalida el viejo; `AuthUser` extractor rechaza token inválido/ausente.
- **Rate-limit:** N intentos rápidos de login → 429.
- `.sqlx` regenerado para los nuevos `query!`; suite offline en verde.

## 7. Entregables F0.3

1. Crate `auth` (password, jwt, tokens, age, code, oauth trait, notify trait, errores) + unit tests.
2. Migración `0010_auth_codes` + repos ampliados (users/auth_identities/refresh_tokens/auth_codes/consent).
3. Impls de DEV de `Notifier` y `OAuthVerifier` + scaffolding de las reales tras config.
4. Endpoints `/auth/*` + extractor `AuthUser` + wiring en `AppState`/`Config`.
5. Rate-limiter en memoria en endpoints de auth.
6. Tests (unit + integración) en verde; `.sqlx` actualizado; CI offline OK.

## 8. Fuera de alcance (fases posteriores)

- Impls reales de Apple/Google/SMS/email con credenciales (se enchufan tras los `trait`s).
- Rate-limiting distribuido con Redis.
- 2FA/biometría/PIN (cliente — F0.4+).
- Cifrado a nivel de campo de datos de salud (F1).
